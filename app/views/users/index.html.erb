<% provide(:title, 'All Users' ) %>
<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<h1>Site Users</h1>
		<%= will_paginate %>
			<%= render @users %>
		<%= will_paginate %>
	</div>
</div>