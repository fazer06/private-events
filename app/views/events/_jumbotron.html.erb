<div class="jumbotron">
	<h1>Hello <%=  current_user.username %></h1>
	<p>You've submitted 
		<%= pluralize(current_user.events.count, "event") %>
	</p>
	<%= link_to new_event_path, class: 'btn btn-success' do %>
		Add A New Event <i class="fa fa-plus-circle"></i>
	<% end %>
</div>